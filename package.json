{
  "name": "canon",
  "version": "1.0.0",
  "description": "LLM-driven universe builder",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:docker": "docker-compose down --remove-orphans && docker system prune -f && docker-compose up --build",
    "dev:docker:clean": "docker-compose down --remove-orphans && docker system prune -af && docker volume prune -f && docker-compose up --build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "packageManager": "npm@9.0.0"
}
